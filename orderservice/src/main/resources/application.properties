# Application name and port
spring.application.name=orderservice
server.port=9000

# Database connection and schema auto-update
spring.datasource.url=jdbc:mysql://localhost:3306/projectorderservice
spring.datasource.username=projectorderservice
spring.datasource.password=order123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Error handling (no stack traces in errors)
server.error.include-stacktrace=never

# Logging levels
logging.level.org.springframework.web=DEBUG