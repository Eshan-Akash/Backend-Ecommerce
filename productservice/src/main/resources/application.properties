# Application name and port
app.serviceProviderId=self

spring.application.name=productservice
server.port=3000

# Database connection and schema auto-update
spring.datasource.url=jdbc:mysql://localhost:3306/projectproductservice
spring.datasource.username=projectproductservice
spring.datasource.password=product123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Error handling (no stack traces in errors)
server.error.include-stacktrace=never

# Logging levels
logging.level.org.springframework.web=DEBUG
